{% for post in posts | reverse %}
  <article
    class="
      card card-compact image-full mt-8 max-w-sm bg-base-100
      group duration-200 outline outline-2 outline-secondary shadow-xl
      hover:z-20 hover:scale-105 hover:rotate-3 hover:outline-8 hover:shadow-2xl
    "
  >
    <figure>
      <img
        class="w-96 aspect-[2/1] duration-200 group-hover:blur"
        src="{% img post.data.image, 480 %}"
        alt="Thumbnail"
      />
    </figure>
    <div class="card-body">
      <div>
        {% set date = post.date %}
        {% set updated = post.data.updated %}
        {% include "post/date.njk" %}
      </div>
      <a
        class="
          card-title px-4 h-full items-center justify-center
          duration-200 group-hover:-rotate-6 group-hover:scale-110
          text-center font-serif font-black hover:text-secondary
        "
        href="{{ post.url | to_lang | url }}"
      >
        <h2>{{ post.data.title | lang_string }}</h2>
      </a>
      <div class="card-actions min-h-[1.25rem] justify-end text-sm sm:text-md">
        {% set tags = post.data.tags %}
        {% include "post/tags.njk" %}
      </div>
    </div>
  </article>
{% endfor %}
