{% for tag in collections.tags | sort %}
  <a
    class="btn btn-cool btn-secondary lg:origin-bottom-left lg:hover:-rotate-6"
    href="{{ ('/tags/' ~ ( tag | slugify ) ~ '/') | to_lang | url }}"
  >#{{ tag }}</a>
{% endfor %}