{% for tag in collections.tags | sort %}
  <a
    class="btn-cool btn-accent btn lg:origin-bottom-left lg:hover:-rotate-6"
    href="{{ ('/tags/' ~ tag ~ '/') | to_lang | url }}"
  >#{{ tag }}</a>
{% endfor %}
