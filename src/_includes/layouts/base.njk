<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ɴɴBlog | {{ title }}</title>
  <link rel="icon" href="https://avatars.githubusercontent.com/u/43980777">
  {% set css %}
    {% include "src/styles/index.css" %}
    {% include "src/styles/prism.css" %}
  {% endset %}
  <style>
    {{ css | postcss | safe }}
  </style>
  <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body>
  <div class="drawer drawer-end">
    <input id="main-drawer" type="checkbox" class="drawer-toggle" />
    <div class="drawer-content relative">
      {% include "navbar.njk" %}
      <div class="min-h-screen">{{ content | safe }}</div>
      {% include "footer.njk" %}
    </div>
    <div class="drawer-side">
      <label for="main-drawer" class="drawer-overlay"></label>
      <nav class="w-80 bg-base-300 bg-opacity-75 backdrop-blur">
        {{ content | toc | safe }}
      </nav>
    </div>
  </div>
</body>
</html>