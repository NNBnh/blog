<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ɴɴBlog | {{ title }}</title>
  <meta name="description" content="{{ description or 'NNB\'s blog' }}">
  <link rel="icon" href="https://avatars.githubusercontent.com/u/43980777">
  {% set css %}
    {% include "src/styles/index.css" %}
    {% include "src/styles/prism.css" %}
  {% endset %}
  <style>
    {{ css | postcss | safe }}
  </style>
</head>
<body class="bg-fixed bg-cover bg-center" style="background-image: url({{ image }});">
  <div class="drawer drawer-end">
    <input id="main-drawer" type="checkbox" class="drawer-toggle" />
    <div class="drawer-content relative">
      {% include "navbar.njk" %}
      {{ content | safe }}
      {% include "footer.njk" %}
    </div>
    {% if content | toc %}
      <label
        for="main-drawer"
        class="
          xl:hidden btn btn-square hover:btn-primary shadow
          fixed bottom-0 right-0 m-4
        "
      >
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path fill-rule="evenodd" d="M3 6.75A.75.75 0 013.75 6h16.5a.75.75 0 010 1.5H3.75A.75.75 0 013 6.75zM3 12a.75.75 0 01.75-.75h16.5a.75.75 0 010 1.5H3.75A.75.75 0 013 12zm8.25 5.25a.75.75 0 01.75-.75h8.25a.75.75 0 010 1.5H12a.75.75 0 01-.75-.75z" clip-rule="evenodd" /></svg>
      </label>
    {% endif %}
    <div class="drawer-side">
      <label for="main-drawer" class="drawer-overlay"></label>
      <div class="w-80 bg-base-300 bg-opacity-75 backdrop-blur shadow">
        {{ content | toc | safe }}
      </div>
    </div>
  </div>
</body>
</html>